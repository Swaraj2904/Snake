using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CameraController : MonoBehaviour {
	//public SnakeController snake;
	//public GameController gameController;
	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		/*Vector3 targetPosition = snakeHead.transform.position + new Vector3 (0, 10, -10);
		Vector3 velocity = Vector3.zero;
		/*
		 * 	SmoothDamp smoothes out the movement of the camera as it tries to reach the target position of the snakeHead
		*
		transform.position = Vector3.SmoothDamp (transform.position, targetPosition, ref velocity, 0.15F);
		*/
	}
}
